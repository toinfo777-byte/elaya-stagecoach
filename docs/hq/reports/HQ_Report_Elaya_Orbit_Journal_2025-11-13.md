# HQ Report — Orbit Journal Bootstrap · 2025-11-13

**Статус:** 🧭 Этап запуска  
**Сервис:** `elaya-stagecoach-web`  
**Фаза:** StageCoach v2 — «Журнал орбиты»

---

## 1) Цель

Создать базу для **временной ленты (timeline)** — накопителя событий ядра (core).  
Переход от памяти-в-RAM к персистентному слою (SQLite → Postgres).  
Фокус: наблюдаемость, циклы, отражения, статистика фазы.

---

## 2) Текущая архитектура

UI ↔ API (/api/status, /api/sync, /api/reflection)
↓
CoreMemory (dict-based)
↓
Timeline (in-memory)

Задача фазы — заменить CoreMemory на устойчивое хранилище (`data.db`).

---

## 3) План реализации

**Шаг 1 — Локальное хранилище**
- [ ] Добавить `app/core/storage.py` с классом `TimelineStore` (SQLite).  
- [ ] Методы: `add_event()`, `get_events(limit, source)`, `purge()`.  
- [ ] Перенести логику `/api/timeline` из оперативной памяти в базу.

**Шаг 2 — Фильтры**
- [ ] Добавить фильтры в `/api/timeline`:  
  - `?source=` (`reflection`, `sync`, `transition`)  
  - `?limit=` (по умолчанию 200).

**Шаг 3 — UI-панель**
- [ ] Новая кнопка: **«Обновить таймлайн»**  
- [ ] Добавить визуальный индикатор цикла (`cycle ⚡`).

**Шаг 4 — Авто-инкремент**
- [ ] При вызове `/api/sync` → `cycle += 1`, событие записывается в БД.

**Шаг 5 — Стабилизация**
- [ ] Валидация guard-ключа для всех POST-эндпоинтов.  
- [ ] Тестирование с PowerShell + curl.  
- [ ] Проверка консистентности данных при перезапуске контейнера.

---

## 4) Вывод

После реализации фазы **«Orbit Journal»** портал Элайи сможет хранить  
историю событий, отражений и циклов между запусками.  
Это станет фундаментом для **фаз наблюдения, хроники и ритма.**

---

## 5) Ключевые файлы

- `app/core/storage.py`
- `app/routes/system.py` → адаптация `/api/timeline`
- `app/templates/timeline.html`
- `docs/hq/reports/HQ_Report_Elaya_Orbit_Journal_2025-11-13.md`

---

🌀 **Следующий этап:** подключение графического таймлайна (`/timeline.html`)  
и визуализация циклов через D3.js или Chart.js.
