# Trainer как тонкий портал

Дата: 2025-11-07  
Статус: ✅ online (web + trainer)

---

## 🎭 Идея

**Trainer** — это только *окно и проводник*.  
Вся логика сцен, контекст и ответы живут в **веб-ядре** (FastAPI).  
Trainer получает события из Telegram, передаёт их в ядро через Core API и возвращает пользователю ответы.

---

## ⚙️ Контракт Core API

Эндпоинты (FastAPI, web-сервис):

| Метод | Путь | Назначение |
|--------|------|------------|
| `POST` | `/api/scene/enter` | вход в цикл, начало сцены |
| `POST` | `/api/scene/reflect` | отражение (ответ пользователя) |
| `POST` | `/api/scene/transition` | переход / завершение цикла |

Хедер авторизации:
