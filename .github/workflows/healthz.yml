name: healthz

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /healthz
        run: |
          set -e
          curl -fsS "${{ secrets.STAGECOACH_WEB_URL }}/healthz" | cat
