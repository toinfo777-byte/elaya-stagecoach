name: Daily Report

on:
  workflow_dispatch:   # 🔹 добавляем возможность запускать вручную
  schedule:
    - cron: "0 7 * * *"  # Ежедневно в 07:00 UTC (10:00 по Мск)

jobs:
  report:
    runs-on: ubuntu-latest

    steps:
      - name: Send daily report to Telegram
        env:
          TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
          TG_REPORT_CHAT_ID: ${{ secrets.TG_REPORT_CHAT_ID }}
        run: |
          echo "📡 Sending report to Telegram..."
          curl -s -X POST https://api.telegram.org/bot${TG_BOT_TOKEN}/sendMessage \
            -d chat_id=${TG_REPORT_CHAT_ID} \
            -d text="📄 *Daily Report* — $(date +'%Y-%m-%d %H:%M:%S')" \
            -d parse_mode=Markdown
