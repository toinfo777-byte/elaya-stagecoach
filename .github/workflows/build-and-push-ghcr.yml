name: Build & Push Docker image to GHCR

on:
  push:
    branches: [ "develop" ]
    paths-ignore:
      - "**.md"
      - "**/.gitignore"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=develop
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Триггерим Render: сначала STAGING, при его отсутствии — PROD.
      # Шаг никогда не валит job, просто пишет HTTP-код.
      - name: Trigger Render deploy (staging → prod fallback, soft)
        if: ${{ success() }}
        env:
          RENDER_STAGING_HOOK: ${{ secrets.RENDER_STAGING_HOOK }}
          RENDER_DEPLOY_HOOK:  ${{ secrets.RENDER_DEPLOY_HOOK }}
        shell: bash
        run: |
          set +e
          HOOK_URL="${RENDER_STAGING_HOOK:-$RENDER_DEPLOY_HOOK}"

          if [ -z "$HOOK_URL" ]; then
            echo "🔹 No Render hook provided. Skipping deploy hook."
            exit 0
          fi

          MASKED="${HOOK_URL%%\?*}?key=***"
          echo "🔹 POST ${MASKED}"
          # Дадим реестру раздать тег
          sleep 10

          # Выполним запрос; выведем тело в STDERR, а код в STDOUT
          HTTP_CODE=$(curl -sS -X POST -o >(cat >&2) -w "%{http_code}" "$HOOK_URL")
          CURL_EXIT=$?

          echo "🔹 curl exit=$CURL_EXIT, http_code=$HTTP_CODE"
          if [ $CURL_EXIT -ne 0 ]; then
            echo "⚠️ Curl error (network/SSL). Не валим pipeline."
          fi
          case "$HTTP_CODE" in
            200|201|202) echo "✅ Render accepted deploy." ;;
            0|"" )       echo "⚠️ No HTTP code captured." ;;
            * )          echo "⚠️ Render returned non-2xx ($HTTP_CODE). Продолжаем." ;;
          esac

          # Никогда не фейлим шаг из-за хука
          exit 0

      # После деплоя — шлём repository_dispatch, чтобы main сделал пост-отчёт
      - name: Dispatch post-deploy status_report
        if: ${{ success() }}
        run: |
          curl -fsS -X POST \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            "${{ github.api_url }}/repos/${{ github.repository }}/dispatches" \
            -d '{"event_type":"status_report","client_payload":{"source":"render_deploy","env":"develop"}}'
