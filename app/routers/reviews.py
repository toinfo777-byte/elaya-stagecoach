from aiogram import Router, F
from aiogram.types import Message

from app.keyboards.main_menu import MAIN_MENU

router = Router(name="reviews-router")


@router.message(F.text.regexp(r"^⭐|^👍|^🔥|^💡"))
async def handle_simple_review(message: Message) -> None:
    """
    Временная версия отзывов.

    Сейчас логика простая:
    — пользователь отправляет сообщение, начинающееся с одного из эмодзи
      (⭐ / 👍 / 🔥 / 💡), можно сразу с коротким текстом.
    — мы благодарим и возвращаем пользователя в главное меню.

    Во вторник здесь появится полноценный сценарий:
    1) выбор эмодзи;
    2) отдельный запрос короткой фразы;
    3) запись отзыва и аккуратный возврат в меню.
    """
    await message.answer(
        "Спасибо за отзыв! Я учту это в дальнейшем развитии Элайи 🌕",
        reply_markup=MAIN_MENU,
    )
